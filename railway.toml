[build]
builder = "NIXPACKS"
buildCommand = "pnpm i --no-frozen-lockfile && pnpm run db:push && pnpm run build"

[deploy]
startCommand = "pnpm run start:server"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
